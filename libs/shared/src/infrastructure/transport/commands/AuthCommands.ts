class AuthCommands {
  static LOGIN = {
    cmd: 'login',
  };

  static VALIDATE_USER = {
    cmd: 'validate-user',
  };

  static CREATE_USER = {
    cmd: 'create-user',
  };
}

export default AuthCommands;
