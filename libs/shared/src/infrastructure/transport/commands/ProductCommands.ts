class ProductCommands {
  static ADD_PRODUCT = {
    cmd: 'add-product',
  };

  static UPDATE_PRODUCT = {
    cmd: 'update-product',
  };

  static DELETE_PRODUCT = {
    cmd: 'delete-product',
  };

  static GET_PRODUCTS = {
    cmd: 'search-products',
  };

  static GET_PRODUCTS_BY_IDS = {
    cmd: 'get-products-by-ids',
  };

  static GET_PRODUCTS_BY_ID = {
    cmd: 'get-products-by-id',
  };

  static SEARCH_PRODUCTS = {
    cmd: 'search-products',
  };
}

export default ProductCommands;
