class SessionCommands {
  static ADD_SESSION = {
    cmd: 'add-session',
  };
  static TERMINATE_SESSION = {
    cmd: 'terminate-session',
  };
  static GET_SESSION_BY = {
    cmd: 'get-session-by',
  };
  static GET_ALL_SESSIONS = {
    cmd: 'get-all-sessions',
  };
  static GET_SESSION = {
    cmd: 'get-session',
  };
}

export default SessionCommands;
